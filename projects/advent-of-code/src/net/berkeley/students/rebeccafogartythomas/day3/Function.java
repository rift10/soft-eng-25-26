package net.berkeley.students.rebeccafogartythomas.day3;

/**
 * Record to represent a function
 * @param place the place in the total string
 * @param function the name of the function - do(), don't(), or mul()
 */
public record Function(int place, String function) {}
